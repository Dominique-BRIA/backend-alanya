- Par defaut on veut 1 compte web à la fois.
- Changer le nom de l'application à Alanya Work, donc recreer egalement une nouvelle application sur firebase.
- Les ecritures sous
- La formation des caracteres
- ajoute le champs agent type varchar(50) dans la table appareil
- creer la table customerChat (ID, appareilID, AlanyaID, start_time, end_time, isFree)

- modifier agent par nomAgent dans Appareil, changer AlanyaID par idAgent reference AlanyaID dans la table Appareil
- ajouter isResolved(boolean) dans la table customerChat
- remplacer AlanyaID par customerID dans la table customerChat
- Dans la table users ajouter le champs appareilTotal (par defaut 1)
- Creer les tables suivantes:
  - table rapportChat (idRapport, idCustomer reference customerChat(ID), idAgent reference users(AlanyaID), resume type TEXT, subject, date, type)
  - table docRapport (idDoc, idRapport, url, type)

- Gerer l'envoie des fichier video vers le mobile jusqu'a present ca sort juste [Text] sur le mobile, et lorsque le mobile envoie un fichier video apres directement apres la reception d'un fichier du web, ca sort sur le mobile [VIDEO] ET LA VIDEO EST RECU COMME FILE SUR LE WEB
- faire passer pseudo de VarChar(100) à (50), personne n'a un pseudo de plus de 50 caracteres, et ca evite les problemes d'affichage sur le mobile.
- fait juste passer publicNumber soit AlanyaPhone passe en VarChar(20)

